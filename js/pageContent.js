const screenWidth = screen.width;
const screenHeight = screen.height;

const navBar = document.querySelector(".navbar");


function checkMovileDevice(){
    if()
}

console.log(screenWidth);
console.log(screenHeight);
